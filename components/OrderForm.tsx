'use client';

import { useState } from 'react';
export default function OrderForm() { const [note, setNote] = useState(''); function order() { const message = note.trim() ? `Hello Gupta Variety Store! I would like to order the following items:\n\n${note.trim()}` : 'Hello Gupta Variety Store! I would like to place an order for groceries and snacks.'; window.open(`https://wa.me/919007182511?text=${encodeURIComponent(message)}`, '_blank'); } return <div className="rounded-[2rem] bg-gradient-to-br from-leaf to-forest p-7 text-white shadow-glow md:p-10"><div className="mx-auto max-w-3xl text-center"><h3 className="text-3xl font-extrabold">Place an order on WhatsApp</h3><p className="mt-2 text-white/80">Tell us what you need and we will prepare it quickly for pickup or delivery.</p><textarea className="mt-5 min-h-28 w-full resize-y rounded-2xl border-0 p-4 text-ink outline-none" value={note} onChange={(event) => setNote(event.target.value)} placeholder="For example: 2 packs chanachur, 1kg muri, 1 bottle mustard oil..." /><button type="button" className="btn-primary mt-4 bg-gold text-ink" onClick={order}>💬 Send WhatsApp Order</button></div></div>; }
